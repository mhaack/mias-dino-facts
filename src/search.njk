---
permalink: search.json
---

[
    {% for dino in collections.dinos %}
        {
            "title" : "{{ dino.data.title }}",
            "url" : "{{ dino.url }}",
            "ingredients" : [{% for ingredient in dino.data.ingredients %}"{{ingredient}}"{% if not loop.last %},{% endif %}{% endfor %}],
            "tags" : [{% for tag in dino.data.tags %}"{{tag}}"{% if not loop.last %},{% endif %}{% endfor %}]
        }{% if not loop.last %},{% endif %}
    {% endfor %}
]